name: Community Context CI

on:
  push:
    branches:
      - 'feature/**'
    paths:
      - 'backend/ctx-content/**'
      - 'backend/aws-cloud-adapter/**'
      - 'backend/domain-core/**'
      - 'backend/pom.xml'
  pull_request:
    branches: [ develop, master ]
    paths:
      - 'backend/ctx-content/**'
      - 'backend/aws-cloud-adapter/**'
      - 'backend/domain-core/**'
      - 'backend/pom.xml'

permissions:
  contents: read
  pull-requests: write
  statuses: write

jobs:
  call-ci-workflow:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      module_name: 'ctx-community'
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}