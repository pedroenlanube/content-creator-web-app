name: Manual Release with POM Version

on:
  workflow_dispatch:
    inputs:
      module:
        description: 'Select the module to release'
        required: true
        type: choice
        options:
          - domain-core
          - cloud-infrastructure-common
          - ctx-content
          - ctx-coaching
          - ctx-user

jobs:
  tag-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Extract version from POM
        id: get_version
        run: |
          MODULE_NAME="${{ github.event.inputs.module }}"
          VERSION=$(mvn -f "${MODULE_NAME}/pom.xml" help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "module_version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Compile and package specific module
        id: package_module
        run: mvn -B package -pl ${{ github.event.inputs.module }} -am -DskipTests

      - name: Create release tag
        id: create_tag
        env:
          MODULE_NAME: ${{ github.event.inputs.module }}
          RELEASE_VERSION: ${{ steps.get_version.outputs.module_version }}
        run: |
          TAG_NAME="${MODULE_NAME}-${RELEASE_VERSION}"
          echo "tag_name=${TAG_NAME}" >> $GITHUB_OUTPUT
          
          # Git configuration required to push tags from Actions
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          git tag $TAG_NAME
          git push origin $TAG_NAME

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.create_tag.outputs.tag_name }}
          release_name: ${{ steps.create_tag.outputs.tag_name }}
          body: |
            🎉 Release for module **${{ github.event.inputs.module }}**
            Version: `${{ steps.get_version.outputs.module_version }}`
          draft: false
          prerelease: false